export enum LoadingModules {
  SIGNIN = "signin",
  SIGNUP = "signup",
  COMMON = "common",
  LOGOUT = "logout",
}

export enum ErrorNamespaces {
  SIGNIN = "signin",
  SIGNUP = "signup",
  COMMON = "common_error",
}
